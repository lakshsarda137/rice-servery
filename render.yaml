services:
  - type: web
    name: rice-servery
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn servery_finder_web:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000

